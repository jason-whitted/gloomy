import React from 'react';

import { CampaignRemoveAchievementDialog } from '../Dialogs';

export default achievement => props => <CampaignRemoveAchievementDialog achievement={achievement} {...props} />;
